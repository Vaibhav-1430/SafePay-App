# Client

SafePay Flutter client currently lives in the repository root (`lib`, `assets`, `android`, `ios`, `web`, etc.).

This folder is kept as a logical marker for the target structure:
- `/client`
- `/server`
- `/ai`
- `/config`
- `/utils`

A physical move can be done in a follow-up without changing runtime behavior.
